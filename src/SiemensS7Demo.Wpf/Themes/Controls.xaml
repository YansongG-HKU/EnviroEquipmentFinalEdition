<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="LabelText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BrushTxt2}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="MonoText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource BrushFontMono}" />
        <Setter Property="Foreground" Value="{StaticResource BrushTxt0}" />
    </Style>

    <Style x:Key="TopBarSurface" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BrushBg2}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushLine1}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Height" Value="56" />
    </Style>

    <Style x:Key="SideNavSurface" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BrushBg2}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushLine1}" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
        <Setter Property="Width" Value="232" />
    </Style>

    <Style x:Key="StatusBarSurface" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BrushBg2}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushLine1}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
        <Setter Property="Height" Value="30" />
    </Style>

    <Style x:Key="DeviceCardSurface" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BrushBg2}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushLine1}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="12" />
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BrushCyan}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushCyan}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Height" Value="32" />
        <Setter Property="Padding" Value="14,0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Medium" />
    </Style>

    <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
        <Setter Property="Background" Value="{StaticResource BrushAlarm}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushAlarm}" />
    </Style>

    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource BrushTxt1}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushLine2}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Height" Value="26" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="FontSize" Value="12" />
    </Style>

    <!-- ── Flat button chrome so Background/Foreground actually render dark (default WPF button
         template paints a light system gradient that ignores our Background). ── -->
    <Style x:Key="FlatButtonBase" TargetType="Button">
        <Setter Property="Foreground" Value="{StaticResource BrushTxt0}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Opacity" Value="0.88" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="bd" Property="Opacity" Value="0.40" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryFlatButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
        <Setter Property="Background" Value="{StaticResource BrushCyan}" />
        <Setter Property="Foreground" Value="#08222A" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushCyan}" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="16,0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="GhostFlatButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
        <Setter Property="Background" Value="{StaticResource BrushBg3}" />
        <Setter Property="Foreground" Value="{StaticResource BrushTxt0}" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushLine2}" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="14,0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
    </Style>

    <Style x:Key="DangerFlatButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
        <Setter Property="Background" Value="{StaticResource BrushAlarm}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderBrush" Value="{StaticResource BrushAlarm}" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="16,0" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
    </Style>

    <!-- Left-nav item button: full-width, left-aligned, transparent; dark hover. -->
    <Style x:Key="NavItemButton" TargetType="Button" BasedOn="{StaticResource FlatButtonBase}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{StaticResource BrushTxt1}" />
        <Setter Property="Height" Value="34" />
        <Setter Property="Padding" Value="14,0" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
    </Style>

    <!-- Card-scoped small uppercase label (muted). -->
    <Style x:Key="CardLabel" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BrushTxt3}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="UnitText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BrushTxt3}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="Margin" Value="3,0,0,3" />
    </Style>

</ResourceDictionary>
