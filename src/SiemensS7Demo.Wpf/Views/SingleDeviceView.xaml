<UserControl x:Class="SiemensS7Demo.Wpf.Views.SingleDeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BrushBg1}">
    <!-- PV/SV hero + command matrix lands in Task 5 (M1.5). Placeholder so Shell DataTemplate resolves. -->
    <TextBlock Text="单设备" Foreground="{StaticResource BrushTxt2}"
               HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" />
</UserControl>
