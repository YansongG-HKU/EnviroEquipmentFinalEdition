<UserControl x:Class="SiemensS7Demo.Wpf.Views.Alarms.HistoryAlarmsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:conv="clr-namespace:SiemensS7Demo.Wpf.Converters"
             Background="{DynamicResource BrushBg1}"
             d:DesignHeight="600" d:DesignWidth="900"
             mc:Ignorable="d">
    <UserControl.Resources>
        <conv:DateTimeOffsetToDateConverter x:Key="DateTimeOffsetToDateConverter" />
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="报警历史"
                   FontSize="22" FontWeight="SemiBold"
                   Foreground="{DynamicResource BrushTxt0}"
                   Margin="0,0,0,12" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="From:" Foreground="{DynamicResource BrushTxt1}" VerticalAlignment="Center" Margin="0,0,4,0" />
            <DatePicker SelectedDate="{Binding FromFilter, Mode=TwoWay,
                                       Converter={StaticResource DateTimeOffsetToDateConverter}}"
                        Width="140" Margin="0,0,12,0" />
            <TextBlock Text="To:" Foreground="{DynamicResource BrushTxt1}" VerticalAlignment="Center" Margin="0,0,4,0" />
            <DatePicker SelectedDate="{Binding ToFilter, Mode=TwoWay,
                                       Converter={StaticResource DateTimeOffsetToDateConverter}}"
                        Width="140" Margin="0,0,12,0" />
            <Button Content="刷新" Command="{Binding RefreshCommand}" Margin="0,0,8,0" Padding="12,4" />
            <Button Content="清空" Command="{Binding ClearFiltersCommand}" Padding="12,4" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Background="{DynamicResource BrushBg2}"
                  Foreground="{DynamicResource BrushTxt0}"
                  RowBackground="{DynamicResource BrushBg2}"
                  AlternatingRowBackground="{DynamicResource BrushBg1}"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="100" />
                <DataGridTextColumn Header="设备" Binding="{Binding DeviceId.Value}" Width="120" />
                <DataGridTextColumn Header="代码" Binding="{Binding Code}" Width="160" />
                <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*" />
                <DataGridTextColumn Header="时间" Binding="{Binding At, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="160" />
                <DataGridCheckBoxColumn Header="Ack" Binding="{Binding Ack}" Width="60" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
