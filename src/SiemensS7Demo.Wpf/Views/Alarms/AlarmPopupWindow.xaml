<Window x:Class="SiemensS7Demo.Wpf.Views.Alarms.AlarmPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="严重报警"
        Height="280" Width="520"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="{DynamicResource BrushBg1}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource BrushAlarm}" CornerRadius="6" Padding="12,6">
            <TextBlock Text="CRITICAL ALARM" FontSize="18" FontWeight="Bold" Foreground="White" />
        </Border>

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <TextBlock Text="{Binding Code}" FontSize="20" FontWeight="SemiBold"
                       Foreground="{DynamicResource BrushTxt0}" />
            <TextBlock Text="{Binding Message}" FontSize="14" TextWrapping="Wrap"
                       Foreground="{DynamicResource BrushTxt1}" Margin="0,8,0,0" />
            <TextBlock Foreground="{DynamicResource BrushTxt3}" Margin="0,12,0,0">
                <Run Text="设备: " />
                <Run Text="{Binding DeviceId.Value, Mode=OneWay}" />
                <Run Text="    时间: " />
                <Run Text="{Binding At, StringFormat=yyyy-MM-dd HH:mm:ss, Mode=OneWay}" />
            </TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="AcknowledgeButton" Content="确认"
                    Click="AcknowledgeButton_Click"
                    Padding="16,6" Margin="0,0,8,0" />
            <Button x:Name="DismissButton" Content="忽略"
                    Click="DismissButton_Click"
                    Padding="16,6" />
        </StackPanel>
    </Grid>
</Window>
