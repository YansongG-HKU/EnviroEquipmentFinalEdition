<UserControl x:Class="SiemensS7Demo.Wpf.Views.OverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BrushBg1}">
    <!-- 9-card grid lands in Task 4 (M1.4). Placeholder so Shell DataTemplate resolves. -->
    <TextBlock Text="总览" Foreground="{StaticResource BrushTxt2}"
               HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" />
</UserControl>
